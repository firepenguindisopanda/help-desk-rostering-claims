# Environment Variables for Help Desk Rosters App

# Flask API Configuration
# Update this to match your Flask API base URL
NEXT_PUBLIC_API_BASE_URL=http://localhost:5000/api

# Example production URLs:
# NEXT_PUBLIC_API_BASE_URL=https://your-flask-api.herokuapp.com/api
# NEXT_PUBLIC_API_BASE_URL=https://api.yourdomain.com

# Legacy variable (kept for compatibility)
NEXT_PUBLIC_SERVER_URL=

# Auth cookie name (HttpOnly cookie set by Flask)
NEXT_PUBLIC_AUTH_COOKIE_NAME=auth_token

# JWT verification (middleware)
# Use either HS* with secret or RS* with JWKS endpoint
JWT_ALG=HS256
# For HS*: set JWT_SECRET
JWT_SECRET=your-super-secret-key-change-this-in-production
# For RS*: set JWKS URL (e.g., https://your-domain/.well-known/jwks.json)
JWT_JWKS_URL=

# Dev-only: mock auth to bypass real cookies/JWT in middleware
# When set to 'mock', middleware will assume authenticated user
# and use NEXT_PUBLIC_AUTH_DEV_ROLE for role enforcement.
# Do NOT enable in production.
NEXT_PUBLIC_AUTH_MODE=
# Options: 'admin' or 'assistant' (used only when AUTH_MODE=mock)
NEXT_PUBLIC_AUTH_DEV_ROLE=assistant